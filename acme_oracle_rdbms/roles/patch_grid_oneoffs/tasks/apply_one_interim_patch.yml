---
# Included once per entry in GRID_INTERIM_PATCHES, with oneoff_item and
# patch_grid_oneoffs_patches_before set by the caller. A block CAN'T carry
# loop: directly in Ansible, so the per-item block/rescue lives here and
# main.yml loops over this file with include_tasks instead.

- name: "Apply Grid interim patch: {{ oneoff_item.label | default(oneoff_item.id) }}"
  when: oneoff_item.id not in (patch_grid_oneoffs_patches_before.stdout | regex_findall('(?m)^([0-9]{5,10});'))
  block:
    - name: Stage this interim patch
      ansible.builtin.include_role:
        name: acme.oracle_rdbms.stage_patch
      vars:
        patch_data: "{{ oneoff_item }}"
        patch_owner: grid

    - name: Apply this interim patch via opatch
      ansible.builtin.command:
        argv: ["{{ deployment_plan.release_cfg.GRID_HOME }}/OPatch/opatch", apply, "{{ oneoff_item.dir }}", -silent]
      changed_when: true
      become: true
      become_user: grid
  rescue:
    - name: "Warn — interim patch error treated as non-fatal"
      ansible.builtin.debug:
        msg: >-
          WARNING — Grid interim patch '{{ oneoff_item.label | default(oneoff_item.id) }}'
          failed or conflicted; the Grid home remains valid at the RU level.
          Review manually: {{ ansible_failed_result.msg | default('see output above') | truncate(300) }}
